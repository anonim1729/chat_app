import ChatLayout from "../layouts/ChatLayout";

const Home = () => {
    return (
        <div className="flex justify-center items-center min-h-screen bg-zinc-600">
            <ChatLayout />
        </div>
    );
};

export default Home;
